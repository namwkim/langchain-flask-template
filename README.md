# Langchain Flask Template


## Development
* Create `.env` and add `OPENAI_API_KEY = [api key]`
* Run the following commands on the terminal
```
pip install -r requirements.txt
python app.py
```
* Client is in the `public` folder
* Backend API routes are in `app.py`

## Deployment

TBD

